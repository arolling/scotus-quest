import { tagfilter } from 'scotus-quest/helpers/tagfilter';
import { module, test } from 'qunit';

module('Unit | Helper | tagfilter');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = tagfilter([42]);
  assert.ok(result);
});
