import { tagCloud } from 'scotus-quest/helpers/tag-cloud';
import { module, test } from 'qunit';

module('Unit | Helper | tag cloud');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = tagCloud([42]);
  assert.ok(result);
});
