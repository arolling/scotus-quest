import { hasAnswers } from 'scotus-quest/helpers/has-answers';
import { module, test } from 'qunit';

module('Unit | Helper | has answers');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = hasAnswers([42]);
  assert.ok(result);
});
